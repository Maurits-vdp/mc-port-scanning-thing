Add a flag for specifying the rate at which to send packets to server to avoid spamming servers and such

Maybe add a flag to allow for manual continuation over the ip iterator? I.e. user has to push enter on their keyboard to handshake the next server?

TODO maybe add a flag for specifying what port to search? I dunno default port I think is good enough

TODO add a protocol specifying flag?

TODO add output file to save server scanning data?

TODO add image base64 conversion?
